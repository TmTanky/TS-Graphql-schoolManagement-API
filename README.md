# TS-Graphql-schoolManagement-API

GraphQL API of school management 

# Development

Run the `server.ts` in `src/server.ts`, and go to `localhost:8000/graphql` to test the API

# ENV

You can find all ENV variables in `src/server.ts`
